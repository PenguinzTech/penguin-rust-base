apiVersion: apps/v1
kind: Deployment
metadata:
  name: rust-server
spec:
  template:
    spec:
      containers:
        - name: rust-server
          resources:
            requests:
              cpu: "1"
              memory: "4Gi"
            limits:
              cpu: "2"
              memory: "8Gi"
